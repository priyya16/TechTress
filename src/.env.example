# This prototype does not require API keys, tokens, or cloud credentials.
# Copy to .env only if you want optional local overrides. NEVER commit .env.

# Optional: Streamlit server port when launching from the CLI
# STREAMLIT_SERVER_PORT=8501
